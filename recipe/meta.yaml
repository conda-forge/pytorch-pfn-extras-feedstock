{% set name = "pytorch-pfn-extras" %}
{% set version = "0.9.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pytorch_pfn_extras-{{ version }}.tar.gz
  sha256: 82acc16094ebbb661e1bc6d80dd5604080f93d514f82cfe229a9420be36930ce

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
    - wheel
  run:
    - numpy
    - packaging
    - python >={{ python_min }}
    - pytorch
    - typing_extensions >=3.10

test:
  imports:
    - pytorch_pfn_extras
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/pfnet/pytorch-pfn-extras
  summary: Supplementary components to accelerate research and development in PyTorch
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - awvwgk
